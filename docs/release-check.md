# Перевірка перед публікацією · 21.09.2026

**Вердикт: придатний для підготовки локальних drafts із перевіреного формату каталогу.** Незалежний агент повторно перевірив виправлення й не знайшов блокерів для такого використання.

- 14 automated tests: реальний каталог, дублікати ID, неповний документ, відсутні секції/status, невідомі labels/workstreams, некоректні milestone/Expected, відсутній project key, невідомі dependencies, повторний запис у існуючу папку.
- Повторний прогін у чистій копії в папці з пробілами: успіх без залежностей від початкового workspace.
- 170 унікальних задач, 1 360 секцій; усі task descriptions звірено з source paragraph slices. Згенерований JSON збігається з demo package.
- Вхідний DOCX незмінний за SHA-256; повторний запуск не перезаписує попередні результати.
- Усі 4 SKILL.md пройшли skill-creator validation.
- Код і текстові матеріали перевірено на очевидні токени/ключі та абсолютні шляхи локального автора; збігів немає.

## Межі перевірки

Живий запис у Linear не тестувався; connector потрібно підключити окремо. Автоматичний reader підтримує GRANDAR layout від 08.09.2026; інші layouts потребують читання/адаптації через скіл.

Повне змістовне рев’ю 170 задач не виконувалося. Приклад містить 154 планові задачі й 16 conditional reserves; у всіх збережено Requires Tech Lead validation. Додаткова незалежна semantic-вибірка: T-M1-001, T-M1-024, T-M3-002, T-M4-014, T-M6-001, T-M7-024. Upstream facts/stories не перевірялися.

До вже зафіксованих source findings додаються: T-M3-002 (API задача містить UI navigation/empty-state критерії) та T-M6-001 (core slice виключає validation, але AC включають negative/error checks). Це питання до канонічного документа; хелпер зберігає їх без прихованої зміни scope.
